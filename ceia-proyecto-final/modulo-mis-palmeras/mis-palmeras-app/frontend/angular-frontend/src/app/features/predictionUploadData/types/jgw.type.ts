export interface JGWType {
    x_pixel_size: number; // Tamaño de píxel en dirección X
    y_rotation: number; // Rotación en dirección Y
    x_rotation: number; // Rotación en dirección X
    y_pixel_size: number; // Tamaño de píxel en dirección Y
    x_origin: number; // Coordenada X del origen
    y_origin: number; // Coordenada Y del origen
}
