# Documentación

<pre>
Genera la documentación de la función en español, pero utiliza el formato en inglés. O sea, utiliza "Args", "Returns", etc. Pon un ejemplo de ejecución. Si es necesario, genera también las clases de errores que puede tirar la función, así como notas adicionales.
</pre>